{% extends 'base.html.twig' %}

{% block title %}Modifier profil{% endblock %}

{% block body %}
    <h3 class="textPurpleDark">Modifier mon profil</h3>

    {{ include('user/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    <div>
        <a href="{{ path('profile', {'user' : app.user.id}) }}">Retour au profil</a>
    </div>

{% endblock %}

{% block javascripts %}
    {{ encore_entry_script_tags('app') }}
    {{ encore_entry_script_tags('fillProfileInput') }}
{% endblock %}