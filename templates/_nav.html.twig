<nav class="navbar navbar-expand-lg navbar-light bg-light">

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <img src="{{ asset('build/images/balloon.png') }}" style="height: 2rem; transform: scaleX(-1);" class="ml-auto mr-2"/>

    <a class="navbar-brand" href="{{ path('home', {'user' : app.user.id}) }}">Bobo-Bidon</a>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('diet', {'user' : app.user.id}) }}">Régime</a>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Symptômes
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="{{ path('charts', {'user' : app.user.id}) }}">Graphiques</a>
                    <a class="dropdown-item" href="{{ path('show_user_symptom', {'user' : app.user.id}) }}">Historique</a>
                </div>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Aliments
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="{{ path('food_index_user') }}">Liste</a>
                    <a class="dropdown-item" href="{{ path('show_user_food', {'user' : app.user.id}) }}">Historique</a>
                </div>
            </li>
        </ul>

        <ul class="navbar-nav ml-auto">
            {% if is_granted('ROLE_ADMIN') %}
                <li>
                    <a class="nav-link" href="{{ path('admin') }}">Gestion</a>
                </li>
            {% endif %}
            {% if app.user %}
                <li>
                    <a class="nav-link" href="{{ path('profile', {'user' : app.user.id}) }}"> Mon profil</a>
                </li>
                <li>
                    <a class="nav-link" href="{{ path('app_logout') }}"> Se déconnecter</a>
                </li>
            {% else %}
                <li>
                    <a class="nav-link" href="{{ path('app_register') }}"> S'inscrire</a>
                </li>

                <li>
                    <a class="nav-link" href="{{ path('app_login') }}"> Se connecter</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>
