{% extends 'base.html.twig' %}

{% block title %}Edit Symptom{% endblock %}

{% block body %}
    <h1>Modifier le symptôme</h1>

    {{ include('symptom/_form.html.twig', {'button_label': 'Enregistrer'}) }}

    <a href="{{ path('symptom_index') }}">Retour à la liste</a>

    {{ include('symptom/_delete_form.html.twig') }}
{% endblock %}
