{% extends 'base.html.twig' %}

{% block title %}Edit Symptom{% endblock %}

{% block body %}
    <h1>Edit Symptom</h1>

    {{ include('symptom/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('symptom_index') }}">back to list</a>

    {{ include('symptom/_delete_form.html.twig') }}
{% endblock %}
